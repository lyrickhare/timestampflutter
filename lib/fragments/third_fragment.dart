import 'package:flutter/material.dart';

class ThirdFragment extends StatelessWidget {
  const ThirdFragment({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Center(
      child: Text("Hello Fragment 1"),
    );
  }

}